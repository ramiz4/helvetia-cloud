import { ErrorCode, ErrorMessages } from '../ErrorCodes';

/**
 * English error messages
 * Reuses the default messages from ErrorCodes for consistency
 */
export const en: Record<ErrorCode, string> = ErrorMessages;
